spring:
  application:
    name: cloud-gateway-zuul
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: cloud-config-scc
eureka:
  client:
    service-url:
      defaultZone: http://eureka.appx.host:8761/eureka/

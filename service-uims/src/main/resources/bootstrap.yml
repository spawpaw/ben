spring:
  application:
    name: service-uims
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: cloud-config-scc
eureka:
  client:
    service-url:
      defaultZone: http://eureka.appx.host:8761/eureka/
